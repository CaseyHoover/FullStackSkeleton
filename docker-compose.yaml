services:
  postgres:
    image: postgres:17-alpine
    restart: unless-stopped
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: healthdash
      POSTGRES_PASSWORD: healthdash
      POSTGRES_DB: healthdash
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
